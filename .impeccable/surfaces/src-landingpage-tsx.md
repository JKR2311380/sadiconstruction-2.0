---
version: 1
slug: "src-landingpage-tsx"
primary_target: "src/LandingPage.tsx"
related_targets:
  - "route:/"
  - "src/features/landing/sitemark/"
---

# Landing — `/`

Mode: Persuade. Audience: screened Planner / PM arriving at an internal product. Job: understand that BOQ phases own time and Longest Path is computed here, then Sign in or Request access.

Proof: the in-app CPM engine (`src/features/scheduling/engine`) runs on a synthetic Clearwater Medical Center PRJ-2024-008 network in the Engine and Proof sections, labeled synthetic. Constraints: no fake org KPIs; no pills or card grids; no purple SaaS; no kicker labels above headings; exactly six sections.

Chosen: Site Mark (binding plan `docs/superpowers/plans/2026-09-23-landing-sitemark-craft-pass.md`). Supersedes the formwork pour sequence, whose files stay in `src/features/landing/formwork/` unmounted.

## Direction contract

THESIS: A building rises in four discrete phases while the page teaches CPM as motion — forward pass, backward pass, float, spine. Refuses hero-header-footer SaaS and the incumbent charcoal-orange directory.

OWN-WORLD: Survey-sheet light greys `#EEF1F0` / `#E2E7E5`, ink `#0E1210`, and a single survey-mark lime `#D4FF4A` used only on the critical spine (always cased in ink on light grounds). Syne display, Manrope body, IBM Plex Mono on measurements only. 2px controls, 2px radius.

STORY: The bill sets the envelope. Three modules, one bill. Criticality is computed. Two seats, one schedule. Push a date and watch the spine move. Enter.

FIRST VIEWPORT: Wordmark as h1, lede, support, Sign in / Request access on the left; full-bleed R3F plane on the right cycling Massing → Structure → Envelope → Fit-out with camera cuts, caption composed into the plane, phase ticks and a pause control.

FORM: Six sections — Hero, Product (scripted demos), Engine (pass choreography), Roles (pictogram mascot), Proof (live recalculation), Close (ink ground).

FINISH: bounded screenshot pass at 1440 and 375, reduced-motion pass, DESIGN.md landing section.
